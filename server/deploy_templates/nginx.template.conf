server {
    listen 80;
    server_name DOMAIN;

    location / {
        proxy_pass http://unix:/tmp/DOMAIN.socket;
        proxy_set_header Host $host;
        proxy_set X-Real-IP $remote_addr;
    }
}
